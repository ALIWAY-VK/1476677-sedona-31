const searchBtn = document.querySelector(".booking-button-search btn btn-second"),
    form = document.querySelector(".form"),
    formInputs = document.querySelectorAll(".form input");
if (form) {
    const a = () => { let r = !0; return formInputs.forEach(e => { e.value || (r = !1) }), r };
    form.classList.remove("form-open"), searchBtn.addEventListener("click", e => { e.preventDefault(), form.classList.toggle("form-open") }), form.addEventListener("submit", e => { a() || (e.preventDefault(), form.classList.remove("error"), form.offsetWidth = form.offsetWidth, form.classList.add("error")) })
}
